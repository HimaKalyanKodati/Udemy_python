#import random module
import random

#use randint(a,b) to randomize between a and b numbers.
rand = random.randint(0,1);

#if rand = 1 then "Heads", if rand = 0 then "Tails".
if rand == 1:
    print("Heads");
else:
    print("Tails");